# Database Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# OPTION 1: Use appsettings.Development.json (Recommended for Development)
# The application will automatically use appsettings.Development.json when ASPNETCORE_ENVIRONMENT=Development
# No additional environment variables needed for development

# OPTION 2: Override connection string with environment variable (Recommended for Production)
# Use double underscore (__) to represent JSON hierarchy in .NET configuration
# ConnectionStrings__DefaultConnection=Server=mysql79.unoeuro.com;Database=insurancerewards_dk_db_main;User=insurancerewards_dk;Password=your_password_here;Port=3306;

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Development
# Options: Development, Staging, Production

# ASP.NET Core URLs (optional, defaults to https://localhost:7174)
# ASPNETCORE_URLS=https://localhost:7174
# ASPNETCORE_HTTPS_PORT=7174

# Frontend Dev Server Port (optional, defaults to 55992)
# DEV_SERVER_PORT=55992
